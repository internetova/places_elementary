import 'package:elementary/elementary.dart';

/// Default Elementary model for PhotoSlider module
class PhotoSliderModel extends ElementaryModel {
  PhotoSliderModel(ErrorHandler errorHandler) : super(errorHandler: errorHandler);
}
